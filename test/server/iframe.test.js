/* @flow */
/* eslint max-lines: off */

import cheerio from 'cheerio';

import { getInstallmentsMiddleware } from '../../server';
import { defaultLogger } from '../../server/lib';

import { getMockReq, getMockRes, mockSDKMeta } from './mocks';

test('load iframe', () => {
    
});

